# server/requirements_server.txt — Optimized for FLUX.1-dev on NVIDIA A40 (48GB)
# Base Image: runpod/pytorch:2.4.0-py3.11-cuda12.4.1

# ── FastAPI / Server ──────────────────────────────────────────────────────────
fastapi
uvicorn[standard]
python-multipart

# ── Core AI / FLUX Stack ───────────────────────────────────────────────────────
# These versions are critical for FLUX support
diffusers>=0.31.0
transformers>=4.45.0
accelerate>=0.34.0
sentencepiece
protobuf
huggingface-hub>=0.25.0
safetensors
bitsandbytes>=0.43.0
peft                  # For future LoRA support
optimum               # For inference acceleration

# ── Image / Vision / CV ────────────────────────────────────────────────────────
opencv-python-headless
Pillow
scipy                 # Required for Gaussian blending and CV filters
timm                  # Required by transformers depth-estimation (DPT/Zoe)

# ── Utilities ──────────────────────────────────────────────────────────────────
PyYAML
tqdm

# ── IP-Adapter ───────────────────────────────────────────────────────────────
# Keep for SDXL backward compatibility
git+https://github.com/tencent-ailab/IP-Adapter.git@main
